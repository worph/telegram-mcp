{
  "telegram": {
    "botToken": "000000000:XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
    "mode": "polling",
    "accessMode": "private",
    "allowedUsers": []
  },
  "target": {
    "transport": "http",
    "url": "http://localhost:9634/mcp",
    "tool": "echo",
    "params": {
      "message": "{{text}}",
      "chatId": "{{chatId}}",
      "username": "{{username}}"
    }
  },
  "chatTargets": [
    {
      "chatIds": ["123456789"],
      "accessMode": "private",
      "allowedUsers": ["123456789"],
      "transport": "http",
      "url": "http://other-mcp:9634/mcp",
      "tool": "query",
      "params": {
        "prompt": "{{text}}",
        "chatId": "{{chatId}}"
      }
    }
  ],
  "server": {
    "port": 9634
  }
}
